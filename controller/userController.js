// Check for duplicate emails
// Subscription is true if new user registration
// Subscription is true if message and selection is true
// Subscription is true if messaage email is duplicate and subscription (userSchema) is already true
// Subscription is true if message email is duplicate and subscribe is not selected (messageSchema)

const people = ['yoshi', 'charles', 'batman']
const ages = ['1', '12', '123']

module.exports = {
    people: people,
    ages
}
